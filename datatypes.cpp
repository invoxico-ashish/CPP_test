#include <iostream>
// #include <camth>
using namespace std;

int main()
{
    double price = 99.99;
    float interestRate = 3.66F; // F is use to define it as float and f  can be f or F
    long fileSize = 90000L;     // L is use to define it as long value and  l can be L or l
    char letter = 'a';
    bool isValid = true;
    auto piceTwo = 999.9999f;   // auto keyword automatically detact the dataType of variable
    unsigned int number = -255; // unsigned is a data type specifier, which only represents non-negative integers
    return 0;
}